# 2026-07-03-10:10 任务汇总

- [x] 资产页面，右上角，资产操作弹窗，之前我让你去掉了默认勾选的添加帐号，把它加回来
- [x] 资产页面，添加资产的时候，账户图标部分，我准备好了一些图标： /home/talyra42/Downloads，你可以查看其中的图标，然后提供一个分类选择弹出层（也是下面弹出的那个框架，不过这次选择的是图标了，左边图标，右边描述），然后对于设置账户图标，可以让用户选择这些图标中的任意一个图标。对于不同的图标，不同的 svg 大小可能不同，后面我可能会和你说图标大小的问题。
- [x] 我需要你为我的软件绘制一个好看的图标，目前仍然是 Flutter 的默认图标；另外，应用安装包的名字需要改一下，改成 verifin-v1.0.0-xxx 这样子。并且应用在手机上展示的名称，应该是 VeriFin，目前是全部小写的。
- [x] 设置页面最下面，可以增加一个灰色的小字，展示当前应用的版本
- [x] 设置页面，不需要同步方式、android 打包的这个卡片，这个东西有点意义不明。
- [x] 对于我的页面的账本、分类管理，或者是下面的那种列表，这个列表应该是全局都用到的吧？这个列表有点问题，上下的内间距有点高了，需要调整一下。
- [x] 对于记账的面板，数字部分，或者是其他操作部分，我需要你增加震动效果，默认是系统的震动大小（就是点击以后，手机会有一个触感），然后设置里面可以提供一个是否打开触感的开关，默认打开。
- [x] 当日交易页面，搜索的下面的筛选，现在两个搜索是分开的，全部帐号、全部分类的这两个筛选项目，应该全部靠左才对，目前全部分类的距离全部账户有点远了。
- [x] 记账的页面，分类反而地方，点击全部，查看所有的分类的时候，下面弹出来的里面，应该是列表，而不是多个Tag，改成一个列表的形式。
